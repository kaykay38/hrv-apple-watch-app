name: iOS starter workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-latest
    env:
      working-directory: ./HRV\ Monitor
      
    steps:
      - uses: mxcl/xcodebuild@v1
