name: iOS starter workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - working-directory: ./HRV\ Monitor
      - uses: mxcl/xcodebuild@v1
